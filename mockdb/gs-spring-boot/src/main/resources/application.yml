spring:
  application:
    # This ends up as the service name in zipkin
    name: mockdb
  zipkin:
    baseUrl: http://0.0.0.0:9411/
  sleuth:
    async:
      enabled: false
    sampler:
      percentage: 1.0
